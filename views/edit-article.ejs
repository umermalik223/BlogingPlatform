<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Article</title>
    <!-- Link to Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <!-- Link to Tailwind CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f7fafc;
        }

        .header {
            background-color: #1a202c;
            color: #edf2f7;
            padding: 20px;
            text-align: center;
        }

        .container {
            max-width: 800px;
            margin: 30px auto;
            padding: 20px;
            background-color: #ffffff;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            border-radius: 12px;
        }

        h1 {
            font-size: 2rem;
            margin-bottom: 20px;
        }

        h2 {
            font-size: 1.5rem;
            margin-bottom: 15px;
            color: #1a202c;
            margin-top: 15px;
        }

        .button {
            background-color: #38b2ac;
            color: #1a202c;
            padding: 10px 15px;
            margin: 10px 0;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 16px;
            font-weight: 600;
            text-decoration: none;
            display: inline-block;
            transition: background-color 0.3s ease;
        }

        .button:hover {
            background-color: #319795;
        }

        .button:active {
            background-color: #2c7a7b;
        }

        .back-button {
            background-color: #2d3748;
            color: #ffffff;
            padding: 10px 15px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 16px;
            font-weight: 600;
            text-decoration: none;
            display: inline-block;
            transition: background-color 0.3s ease;
        }

        .back-button:hover {
            background-color: #4a5568;
        }

        .back-button:active {
            background-color: #1a202c;
        }

        label {
            color: #2d3748;
        }
    </style>
</head>

<body>

    <!-- Header Section -->
    <header class="header">
        <h1>Edit Article</h1>
    </header>

    <!-- Main Content Section -->
    <main class="container">
        <!-- Article Information Section -->
        <section>
            <h2>Article Information</h2>
            <div>
                <p><span>Created:</span> <%= article.created_at %></p>
                <p><span>Last Modified:</span> <%= article.last_modified %></p>
            </div>
        </section>

        <!-- Article Form Section -->
        <section>
            <h2>Edit Article</h2>
            <form action="/author/edit-article/<%= article.article_id %>" method="POST">
                <div>
                    <label for="title">Title</label>
                    <input type="text" id="title" name="title" value="<%= article.title %>"
                        class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-teal-500 focus:border-teal-500 sm:text-sm">
                </div>
                <div class="mt-4">
                    <label for="content">Content</label>
                    <textarea id="content" name="content" rows="8"
                        class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-teal-500 focus:border-teal-500 sm:text-sm"><%= article.content %></textarea>
                </div>
                <div class="mt-4">
                    <button type="submit" class="button">Submit Changes</button>
                </div>
            </form>
        </section>

        <!-- Back Button -->
        <div class="flex justify-start mt-4">
            <a href="/author/home" class="back-button">Back</a>
        </div>

    </main>

</body>

</html>
